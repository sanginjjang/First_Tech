<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.dao.sangin.ICompanyApplicationManagementDao">
    <select id="companyApplicationList" resultType="com.example.demo.dto.ApplicationDto">
        SELECT * FROM applications WHERE companyId = #{companyId}
    </select>
</mapper>
